# Supabase Configuration
# Get these values from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (CRITICAL - Keep Secret!)
# Required for admin operations like creating users programmatically
# Found in: Supabase Dashboard > Settings > API > service_role key
# WARNING: This key bypasses Row Level Security. Never expose to client!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application URL
# In production (Railway), this should be your deployed URL: https://ekkle.com.br
# In development, this should be: http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Configuration (Resend)
# Sign up at https://resend.com and get your API key
# Required for sending welcome emails to new leaders
RESEND_API_KEY=re_your_api_key_here

# Email sender address (must be verified in Resend)
# In development: Can use onboarding@resend.dev (sandbox)
# In production: Use your verified domain (e.g., noreply@ekkle.com.br)
FROM_EMAIL=onboarding@resend.dev

# =====================================================
# Stripe Configuration (Payment Processing)
# =====================================================
# Sign up at https://stripe.com and get your API keys
# Dashboard: https://dashboard.stripe.com

# Secret Key - Used for server-side API calls
# Found in: Dashboard > Developers > API keys
# Format: sk_live_xxx (production) or sk_test_xxx (test mode)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Webhook Secret - Used to validate webhook signatures
# Configure in: Dashboard > Developers > Webhooks
# Set the webhook URL to: https://your-domain.com/api/webhooks/stripe
# Select events: invoice.paid, invoice.payment_failed, customer.subscription.*
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# =====================================================
# WhatsApp AI Agent Configuration
# =====================================================

# Twilio WhatsApp (System ↔ Pastor communication)
# Sign up at https://www.twilio.com/console
# Required for WhatsApp AI Agent to communicate with pastors
TWILIO_ACCOUNT_SID=AC_your_account_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
# Twilio WhatsApp number (sandbox or production)
# Sandbox: whatsapp:+14155238886
# Production: whatsapp:+your_whatsapp_business_number
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Twilio Webhook Secret (optional, for signature validation)
# Generate a random secret for additional security
TWILIO_WEBHOOK_SECRET=your_webhook_secret_here

# OpenAI Configuration (AI Intelligence)
# Sign up at https://platform.openai.com
# Required for conversational AI capabilities
OPENAI_API_KEY=sk-your-openai-api-key-here
# Model to use (default: gpt-4o-mini - 33x cheaper than gpt-4o)
# gpt-4o-mini: ~$0.15/1M tokens (recommended for production)
# gpt-4o: ~$5/1M tokens (only if you need highest quality)
OPENAI_MODEL=gpt-4o-mini

# =====================================================
# Evolution API Configuration (WhatsApp Business)
# =====================================================
# Evolution API for WhatsApp integration
# Get these from your Evolution API instance
EVOLUTION_API_URL=https://your-evolution-api-url.com
EVOLUTION_API_KEY=your-evolution-api-key-here

# =====================================================
# Upstash Redis Configuration (Rate Limiting)
# =====================================================
# Sign up at https://upstash.com and create a Redis database
# Required for distributed rate limiting across multiple instances
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# =====================================================
# API.Bible Configuration (Bible Content)
# =====================================================
# Sign up at https://scripture.api.bible/signup
# Required for Bible reading features (passages, chapters, verses)
# Free tier includes 5,000 API calls per day
API_BIBLE_KEY=your-api-bible-key-here

# =====================================================
# Daily.co Configuration (Embedded Video Calls)
# =====================================================
# Sign up at https://www.daily.co and create an account
# Dashboard: https://dashboard.daily.co
# Required for embedded video calls in prayer rooms
# Benefits: Video calls happen inside your app (no Zoom redirect)
# Free tier: 2,000 participant minutes/month
DAILY_API_KEY=your-daily-api-key-here
# Your Daily.co subdomain (e.g., 'myapp' for myapp.daily.co)
DAILY_DOMAIN=your-daily-domain

# =====================================================
# Zoom Configuration (Legacy - Optional)
# =====================================================
# Only needed if you want to use Zoom instead of Daily.co
# Sign up at https://marketplace.zoom.us and create a Server-to-Server OAuth app
# Required scopes: meeting:write, meeting:read
ZOOM_CLIENT_ID=your-zoom-client-id
ZOOM_CLIENT_SECRET=your-zoom-client-secret
ZOOM_ACCOUNT_ID=your-zoom-account-id

# =====================================================
# LiveKit Configuration (Browser-based Live Streaming)
# =====================================================
# Sign up at https://cloud.livekit.io and create a project
# Dashboard: https://cloud.livekit.io
# Required for browser-based live streaming (pastor transmits directly from browser)
# Free tier: 10,000 participant minutes/month

# LiveKit Cloud URL (WebSocket URL for your project)
# Found in: LiveKit Dashboard > Your Project > Settings
# Format: wss://your-project-id.livekit.cloud
LIVEKIT_URL=wss://your-project.livekit.cloud

# LiveKit API Key
# Found in: LiveKit Dashboard > Your Project > Settings > API Keys
LIVEKIT_API_KEY=your-livekit-api-key

# LiveKit API Secret
# Found in: LiveKit Dashboard > Your Project > Settings > API Keys
# WARNING: Keep this secret! Never expose to client!
LIVEKIT_API_SECRET=your-livekit-api-secret

# =====================================================
# Mux Configuration (Video Streaming & Distribution)
# =====================================================
# Sign up at https://www.mux.com and create an account
# Dashboard: https://dashboard.mux.com
# Required for video processing, storage, and CDN delivery
# Free tier: 100,000 delivery minutes/month

# Mux Token ID
# Found in: Mux Dashboard > Settings > API Access Tokens
MUX_TOKEN_ID=your-mux-token-id

# Mux Token Secret
# Found in: Mux Dashboard > Settings > API Access Tokens
# WARNING: Keep this secret! Never expose to client!
MUX_TOKEN_SECRET=your-mux-token-secret
